type readAndDeployFileResultProps = {
  isExistsSync: boolean;
  config: any;
};

type addParamArrItemProps = {
  authJSON: any;
  paramArr: any;
};

type mkdirSyncCheckProps = {
  appName: string;
  isMkdir: boolean;
};

type buildCommandImplementProps = {
  appName: string;
  isExistsFile: boolean;
};
